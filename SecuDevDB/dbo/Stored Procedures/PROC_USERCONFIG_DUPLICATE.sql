CREATE PROCEDURE [dbo].[PROC_USERCONFIG_DUPLICATE]
	@UID VARCHAR(500) = ''
AS
BEGIN
	
	DECLARE @Rtn INT = 0

	IF NOT EXISTS (SELECT UID FROM Users WHERE UID = @UID)
	BEGIN

		SET @Rtn = 1;

	END

	RETURN @Rtn

END